import { StyleSheet } from 'react-native';

export const formStyle = StyleSheet.create({
    formStyle: {
        display: 'flex',
        width: '100%',
        // alignItems: 'center',
        justifyContent: 'center',
        rowGap: 16,
    },
});
